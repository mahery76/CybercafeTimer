import React, { useRef, useState } from "react";

function ComputerList({ terminalList, handleDelete }) {
    const [isSelected, setIsSelected] = useState(true)
    const computer_name = useRef(null)

  return (
    <>
  
    </>
  );
}

export default ComputerList;
